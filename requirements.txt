# Salt testing (optional - for integration marker tests only)
# NOTE: Salt has compatibility issues with Python 3.10+
# For unit tests without Salt, comment these out:
salt==3006.16                    # Salt core for testing
msgpack>=1.0.0
looseversion>=1.3.0             # Salt dependency for version handling
distro>=1.0.1
Jinja2>=3.1.5